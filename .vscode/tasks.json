{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build restapp",
            "type": "shell",
            "command": "cd scripts && source .venv/bin/activate && python3 build_cdk.py ",
            "problemMatcher": [],
            "presentation": {
                "reveal": "always"
            }
        },
        {
            "label": "Deploy CDK",
            "type": "shell",
            "command": "cd cdk-app && source .venv/bin/activate && cdk deploy --no-version-reporting -a cdk.out/${input:assemblyName} -e --all --require-approval never",
            "presentation": {
                "reveal": "always"
            },
            "problemMatcher": []
        }
    ]
}